{
  "name": "qat-backend",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start-pm2-dev": "pm2 restart pm2.dev.config.cjs --interpreter ~/.bun/bin/bun--update-env",
    "start-pm2-prod": "pm2 restart pm2.prod.config.cjs  --interpreter --update-env"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.5",
    "@types/socket.io": "^3.0.2",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "jose": "^5.2.0",
    "mongoose": "^8.0.3",
    "socket.io": "^4.7.2"
  }
}
